# RISC-V 与嵌入式系统

*RISC-V 是正在重塑芯片行业的开源指令集架构。本节涵盖 RISC-V 理念、V 向量扩展、嵌入式机器学习推理、微控制器上的 TinyML、AI 加速器中的 RISC-V 与边缘部署约束。*

- 至今介绍的每种芯片架构（x86、ARM）都需要**许可证**。Intel 和 AMD 为 x86 付费；Apple、Qualcomm 和每家智能手机厂商每年向 ARM 支付数十亿美元。**RISC-V** 不同：它是开放标准。任何人都能设计、制造和销售 RISC-V 芯片，无需向任何人支付版税。这正在改变芯片设计的经济性，尤其是 AI 芯片。

!!! note "大白话"
    RISC-V 提供的是公开的“机器指令语言规范”，不是一颗固定芯片。任何厂商都能据此造自己的芯片，再把成本和设计自由度用在真正需要的地方。

## RISC-V 理念

- RISC-V（读作“risk five”）由 UC Berkeley 于 2010 年创建，是干净、现代的 RISC 指令集。其关键原则：

    - **开放标准**：ISA 规范免费提供。无需许可费、保密协议或法律协议即可构建 RISC-V CPU。这类似于 Linux 对操作系统的意义，任何人都可使用、修改并在其上构建。

    - **模块化设计**：基础 ISA（RV32I 或 RV64I）极小，仅 47 条指令。其余全部是可选的**扩展**：M（乘/除）、A（原子操作）、F/D（浮点）、C（压缩指令）、V（向量处理）。只选择需要的部分，使芯片小且高效。

    - **没有历史包袱**：x86 背负 45 年向后兼容，ARM 背负 35 年，RISC-V 从零开始，吸取二者经验。没有只为兼容 1980 年代软件而存在的晦涩指令。

- **谁在使用 RISC-V**：SiFive（通用核心）、阿里巴巴（玄铁服务器核心）、Western Digital（存储控制器，已出货数十亿颗）、Espressif（ESP32-C3，流行 IoT 芯片），以及数十家用 RISC-V 管理自定义计算单元的 AI 加速器初创公司。

!!! note "大白话"
    它的价值不在于今天全面击败 x86 或 ARM，而在于从小型 IoT 芯片到 AI 加速器控制核都能按需求裁剪。规模越大、差异化越强，免授权费和可定制的意义越明显。

## RISC-V 基础架构

- 基础整数 ISA（64 位 RV64I）具有：
    - **32 个通用寄存器**（x0-x31，各 64 位）。x0 被硬连线为零，便于无需特殊指令实现常见模式。
    - **固定 32 位指令宽度**。C 扩展增加 16 位压缩指令以提高代码密度。
    - **加载-存储架构**：和 ARM 一样，算术仅作用于寄存器，内存访问经显式加载/存储指令完成。

```
# RISC-V assembly (for flavour — you will use C/C++)
add  x3, x1, x2      # x3 = x1 + x2
lw   x4, 0(x5)       # load word from address in x5
sw   x4, 8(x5)       # store word to address x5 + 8
beq  x1, x2, label   # branch if x1 == x2
```

- ISA 的简单性使 RISC-V 核小且高效。最小 RV32I 核可用约 10,000 个门实现（ARM Cortex-M0 约 12,000 个）。在嵌入式系统中，每毫瓦功耗和每平方毫米硅片都很重要，这一点至关重要。

!!! note "大白话"
    RISC-V 基础指令集很小，芯片不必带一大堆用不到的电路。对电池设备和廉价传感器而言，少一点面积、少一点功耗都可能决定产品能不能成立。

## V 扩展：RISC-V 向量处理

- **V 扩展**（RVV）为 RISC-V 增加可伸缩向量处理，类似 ARM SVE。向量寄存器长度（VLEN）可由硬件配置，范围为 128 至 65,536 位。代码按**向量长度无关**方式编写，无需重新编译即可在任何 VLEN 上运行。

```c
#include <riscv_vector.h>

// Vector addition using RVV intrinsics
void vadd_rvv(const float* a, const float* b, float* c, int n) {
    while (n > 0) {
        // vsetvl: set vector length — processes min(n, hardware_max) elements
        size_t vl = __riscv_vsetvl_e32m1(n);

        // Load vl elements
        vfloat32m1_t va = __riscv_vle32_v_f32m1(a, vl);
        vfloat32m1_t vb = __riscv_vle32_v_f32m1(b, vl);

        // Add
        vfloat32m1_t vc = __riscv_vfadd_vv_f32m1(va, vb, vl);

        // Store
        __riscv_vse32_v_f32m1(c, vc, vl);

        // Advance pointers
        a += vl; b += vl; c += vl; n -= vl;
    }
}
```

- **`vsetvl`** 是关键指令。它向硬件说“我想处理这么多元素”，硬件答复“我能处理这么多”（受 VLEN 上限约束）。循环自动适配任意向量宽度，无需标量清理，最后一次迭代只处理较少元素。

!!! note "大白话"
    RVV 每轮先问芯片这次能并行处理多少个元素，再按这个数量推进指针。最后剩多少就处理多少，因此同一循环不需要预先知道寄存器到底有多宽。

- **LMUL**（长度乘数）：RVV 可将多个向量寄存器分组（m1、m2、m4、m8），以每条指令处理更多元素，代价是可用寄存器更少。`m1` 每个向量操作数用一个寄存器；`m8` 用八个寄存器，处理 8 倍元素，但只剩 4 组寄存器可用。

!!! note "大白话"
    LMUL 是“每次多装货”与“手边可用货架变少”的交换。块太大可能减少循环次数，但也更容易让寄存器不够用，必须结合实际内核选择。

- 相比 x86 AVX 的固定 256/512 位和 ARM NEON 的固定 128 位，RVV 的可伸缩性对多样硬件具有主要优势：同一代码可运行在微小嵌入式核心（VLEN=128）与高性能服务器核心（VLEN=1024+）上。

!!! note "大白话"
    RVV 的可移植性不是让窄芯片变成宽芯片，而是让同一算法自动吃满各自拥有的宽度。这样厂商能做不同规格的芯片，软件不必为每个规格复制一份向量循环。

## 嵌入式机器学习：TinyML

- **TinyML** 是在微控制器上运行机器学习。这些设备只有 KB 级 RAM、MHz 级 CPU 和毫瓦级功耗。比如：识别关键词（“Hey Siri”）的传感器、分类手势的加速度计，或统计人数的相机，均在一颗成本 $0.50、没有互联网连接的芯片上运行。

!!! note "大白话"
    TinyML 不是把服务器模型原样塞进小设备，而是从模型大小、计算量和内存占用开始重新设计。它用极小资源换取离线、低延迟和低功耗。

- 约束极端：

| 资源 | 服务器 GPU | 智能手机 | 微控制器 |
|----------|-----------|------------|-----------------|
| RAM | 80 GB | 6 GB | 256 KB |
| 存储 | TB | 128 GB | 1 MB |
| 计算 | 1000 TFLOPS | 10 TFLOPS | 0.001 TFLOPS |
| 功耗 | 700 W | 5 W | 0.001 W |
| 成本 | $30,000 | $500 | $1 |

- 能放进服务器 GPU 的模型（$O(10^{10})$ 参数）无法放进微控制器。TinyML 模型只有 $O(10^4)$ 至 $O(10^6)$ 参数，使用 INT8 乃至 INT4 量化。

!!! note "大白话"
    微控制器的 RAM 只有几百 KB，连一张大图都未必装得下，更不可能放数十亿参数。量化和小网络不是可选优化，而是让模型能启动的前提。

### TensorFlow Lite Micro（TFLM）

- **TFLM** 是 Google 的微控制器推理框架。它运行量化 TensorFlow Lite 模型，不使用动态内存分配、不依赖操作系统，二进制体积约 20 KB。

```cpp
// TinyML inference on a microcontroller (simplified)
#include "tensorflow/lite/micro/micro_interpreter.h"
#include "tensorflow/lite/micro/micro_mutable_op_resolver.h"

// Model is compiled into a C array (const unsigned char model_data[])
const tflite::Model* model = tflite::GetModel(model_data);

// Allocate a fixed memory arena (no malloc!)
constexpr int kArenaSize = 10 * 1024;  // 10 KB
uint8_t tensor_arena[kArenaSize];

// Set up interpreter
tflite::MicroInterpreter interpreter(model, resolver, tensor_arena, kArenaSize);
interpreter.AllocateTensors();

// Set input
float* input = interpreter.input(0)->data.f;
input[0] = sensor_reading;

// Run inference
interpreter.Invoke();

// Read output
float* output = interpreter.output(0)->data.f;
if (output[0] > 0.8f) {
    trigger_alert();
}
```

- **代码中的关键约束**：
    - `tensor_arena` 静态分配，无 `malloc`、无堆。嵌入式系统往往没有动态内存分配器。
    - 模型是存放在 flash（ROM）而非从文件系统加载的 `const` 字节数组。
    - 整个框架 + 模型 + 运行时只需数十 KB。

!!! note "大白话"
    嵌入式推理常在编译时就把模型和缓冲区大小定死，运行时不能随意 `malloc` 或读文件。这样可预测、可靠，但你必须提前精确算清每一字节。

### 边缘端模型优化

- 要让模型运行在微控制器上，需要激进优化：

    - **量化**（第 18 章）：将 float32 权重转为 INT8，体积缩小 4 倍，在纯整数硬件上快 2 至 4 倍。训练后量化简单；量化感知训练保留更多准确率。

    - **剪枝**：移除接近零的权重。结构化剪枝（移除完整通道/头）比非结构化剪枝（随机零）更适合硬件，因为它减少实际计算，而不只是存储。

    - **知识蒸馏**（第 6 章）：训练小型“学生”模型模仿大型“教师”模型。学生从教师的软预测中学习，准确率高于从头训练。

    - **神经架构搜索（NAS）**：自动搜索满足硬件预算（延迟、内存、功耗）的高效架构。**MicroNets** 和 **MCUNet** 可为具体微控制器寻找优化架构。

    - **算子融合**：将 conv + batch norm + ReLU 合为单个融合操作，消除中间内存写入。原理同 GPU 内核融合，但仅有 256 KB RAM 时更关键。

!!! note "大白话"
    量化压缩数值，剪枝和蒸馏缩小网络，NAS 按硬件预算找结构，融合减少临时内存。通常要多种方法一起用，单靠一种技巧很难让模型进入极小设备。

## AI 加速器中的 RISC-V

- 很多 AI 加速器初创公司不直接用 RISC-V 运行机器学习模型，而是把它作为管理自定义计算单元的**控制处理器**：

```
┌─────────────────────────────────────────┐
│              AI Accelerator             │
│                                         │
│  ┌──────────┐    ┌──────────────────┐   │
│  │  RISC-V  │───→│  Custom Matrix   │   │
│  │  Control │    │  Multiply Unit   │   │
│  │  Core    │    │  (systolic array,│   │
│  │          │    │  custom dataflow)│   │
│  └──────────┘    └──────────────────┘   │
│       │                    │            │
│       ▼                    ▼            │
│  ┌──────────┐    ┌──────────────────┐   │
│  │  Memory  │    │  On-chip SRAM    │   │
│  │  Control │    │  (activation     │   │
│  │          │    │   buffer)        │   │
│  └──────────┘    └──────────────────┘   │
└─────────────────────────────────────────┘
```

- RISC-V 核负责：从外部内存加载模型权重、调度层执行、管理计算单元间的数据流，以及通过 PCIe、USB 或 SPI 与主机通信。繁重计算（矩阵乘法、卷积）由自定义硬件完成，而非 RISC-V 核。

!!! note "大白话"
    很多 AI 芯片里的 RISC-V 更像调度员：安排数据、启动专用矩阵单元、和外部设备通信。真正最重的乘法由旁边定制的硬件做，不靠通用控制核硬算。

- **为何用 RISC-V 做控制**：无需许可费，对初创企业至关重要；可定制，能加入领域专用指令；占用小，控制核不需要 x86 的复杂性；开放生态支持快速原型。

!!! note "大白话"
    控制处理器通常不需要顶级单核性能，却需要便宜、可改和易集成。RISC-V 正好适合这个位置，能把芯片的复杂资源留给真正的加速单元。

- **实例**：Esperanto Technologies（用于机器学习的 1000+ 个 RISC-V 核）、Tenstorrent（RISC-V 控制 + 自定义 tensix 核）、SiFive（面向边缘机器学习、带向量扩展的 RISC-V 核）。

## 边缘部署约束

- 在边缘端（设备上而非云端）部署机器学习会带来云部署没有的约束：

- **功耗**：电池设备的总功耗预算可能只有 100 mW。一个模型消耗 50 mW，系统其余部分（传感器、无线电、显示器）只剩 50 mW。功耗感知推理会调度计算，以避免热降频并延长续航。

- **延迟**：边缘推理常须实时。唤醒词检测器（“Hey Siri”）必须在约 200 ms 内响应；自动驾驶感知系统（第 11 章）必须在约 30 ms 内处理帧。到云端的网络往返（50-200 ms）对这些用例太慢。

- **隐私**：在设备上处理数据意味着敏感数据（医学图像、录音、个人照片）永不离开设备。在某些司法辖区，这是法律要求（GDPR），在所有地方则是用户信任要求。

- **连接性**：边缘设备可能网络断续甚至完全没有网络。火星车（第 11 章）、潜艇或农村农场传感器中的模型必须完全离线工作。

- **规模化成本**：向十亿台智能手机部署机器学习，每台成本为 $0，因为硬件已存在；向十亿个 IoT 传感器部署则意味着每个传感器的机器学习硬件预算只有几分钱。RISC-V 的零许可成本在这种规模上意义巨大。

!!! note "大白话"
    边缘部署同时被电池、响应时间、隐私、断网和单机成本约束。云端能靠加服务器解决的问题，在一颗几分钱的芯片上必须靠模型和硬件设计一起解决。

## 编程任务（使用 g++ 或 riscv64-gcc 交叉编译器）

1. 编写模拟 TinyML 推理管道的 C 程序：静态分配模型缓冲区、运行模拟前向传播、测量资源使用。这教授嵌入式约束，即无 malloc、固定内存 arena。
```cpp
// task1_tinyml_sim.cpp
// Compile: g++ -O2 -o task1 task1_tinyml_sim.cpp

#include <iostream>
#include <chrono>
#include <cmath>
#include <cstring>

// Simulate a microcontroller: fixed memory arena, no dynamic allocation
static constexpr int ARENA_SIZE = 32 * 1024;  // 32 KB total RAM budget
static uint8_t arena[ARENA_SIZE];

// Simple 2-layer MLP: 784 -> 64 -> 10 (MNIST-like, INT8 weights)
struct TinyModel {
    int8_t w1[784 * 64];      // layer 1 weights: 50,176 bytes
    int8_t b1[64];             // layer 1 biases
    int8_t w2[64 * 10];       // layer 2 weights: 640 bytes
    int8_t b2[10];             // layer 2 biases
    // Total: ~51 KB → must go in flash (ROM), not RAM
};

// Check if model fits in flash
void check_model_fit(int flash_kb) {
    int model_bytes = sizeof(TinyModel);
    std::cout << "Model size: " << model_bytes << " bytes ("
              << model_bytes / 1024 << " KB)\n";
    std::cout << "Flash: " << flash_kb << " KB → "
              << (model_bytes <= flash_kb * 1024 ? "FITS" : "TOO LARGE") << "\n";
}

// Mock inference using the fixed arena for activations
void mock_inference(const int8_t* input, int8_t* output) {
    // Activations go in the arena (RAM), not allocated dynamically
    int8_t* act1 = (int8_t*)arena;            // 64 bytes for layer 1 output
    int8_t* act2 = (int8_t*)(arena + 64);     // 10 bytes for layer 2 output

    // Layer 1: simplified matmul (not real quantised matmul, just structure demo)
    for (int j = 0; j < 64; j++) {
        int32_t sum = 0;  // accumulate in int32 to avoid overflow
        for (int i = 0; i < 784; i++) {
            sum += (int32_t)input[i] * 1;  // mock: weight = 1
        }
        act1[j] = (int8_t)std::max(-128, std::min(127, sum / 784));  // quantise back
        act1[j] = act1[j] > 0 ? act1[j] : 0;  // ReLU
    }

    // Layer 2
    for (int j = 0; j < 10; j++) {
        int32_t sum = 0;
        for (int i = 0; i < 64; i++) {
            sum += (int32_t)act1[i] * 1;
        }
        act2[j] = (int8_t)std::max(-128, std::min(127, sum / 64));
    }

    std::memcpy(output, act2, 10);
}

int main() {
    std::cout << "=== TinyML Resource Budget ===\n";
    std::cout << "Arena (RAM): " << ARENA_SIZE << " bytes ("
              << ARENA_SIZE / 1024 << " KB)\n";
    check_model_fit(256);  // typical MCU flash

    // Activation memory used
    int activation_bytes = 64 + 10;  // layer 1 + layer 2 outputs
    std::cout << "Activation memory: " << activation_bytes
              << " bytes / " << ARENA_SIZE << " available\n\n";

    // Benchmark inference
    int8_t input[784];
    int8_t output[10];
    std::memset(input, 1, 784);

    auto start = std::chrono::high_resolution_clock::now();
    for (int i = 0; i < 10000; i++) {
        mock_inference(input, output);
    }
    auto end = std::chrono::high_resolution_clock::now();
    double us = std::chrono::duration<double, std::micro>(end - start).count() / 10000;

    std::cout << "Inference latency: " << us << " us\n";
    std::cout << "At 160 MHz MCU (~6.25 ns/cycle): ~"
              << (int)(us * 160) << " cycles\n";

    std::cout << "Output logits: ";
    for (int i = 0; i < 10; i++) std::cout << (int)output[i] << " ";
    std::cout << "\n";

    return 0;
}
```

2. 编写将 float32 权重量化为 INT8 的 C++ 程序，测量压缩率与量化误差。
```cpp
// task2_quantise.cpp
// Compile: g++ -O3 -o task2 task2_quantise.cpp

#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
#include <numeric>

// Symmetric quantisation: map float range [-max, +max] to [-127, +127]
void quantise_symmetric(const float* input, int8_t* output, int n, float& scale) {
    float max_val = 0.0f;
    for (int i = 0; i < n; i++) {
        max_val = std::max(max_val, std::abs(input[i]));
    }
    scale = max_val / 127.0f;
    for (int i = 0; i < n; i++) {
        float scaled = input[i] / scale;
        output[i] = (int8_t)std::max(-127.0f, std::min(127.0f, std::round(scaled)));
    }
}

// Dequantise: INT8 back to float
void dequantise(const int8_t* input, float* output, int n, float scale) {
    for (int i = 0; i < n; i++) {
        output[i] = (float)input[i] * scale;
    }
}

int main() {
    const int N = 100000;

    // Simulate random weights (roughly normal distribution)
    std::vector<float> weights(N);
    for (int i = 0; i < N; i++) {
        // Simple pseudo-random normal-ish values
        float u1 = (float)(i * 7 % 997 + 1) / 998.0f;
        float u2 = (float)(i * 13 % 991 + 1) / 992.0f;
        weights[i] = std::sqrt(-2.0f * std::log(u1)) * std::cos(6.2832f * u2) * 0.1f;
    }

    // Quantise
    std::vector<int8_t> quantised(N);
    float scale;
    quantise_symmetric(weights.data(), quantised.data(), N, scale);

    // Dequantise and measure error
    std::vector<float> reconstructed(N);
    dequantise(quantised.data(), reconstructed.data(), N, scale);

    float max_error = 0.0f, total_error = 0.0f;
    for (int i = 0; i < N; i++) {
        float err = std::abs(weights[i] - reconstructed[i]);
        max_error = std::max(max_error, err);
        total_error += err;
    }

    std::cout << "=== Quantisation Results ===\n";
    std::cout << "Original:    " << N * 4 << " bytes (float32)\n";
    std::cout << "Quantised:   " << N * 1 << " bytes (int8) + 4 bytes (scale)\n";
    std::cout << "Compression: " << 4.0f << "x\n";
    std::cout << "Scale factor: " << scale << "\n";
    std::cout << "Mean abs error: " << total_error / N << "\n";
    std::cout << "Max abs error:  " << max_error << "\n";
    std::cout << "Max abs error / scale: " << max_error / scale
              << " (should be <= 0.5 quantisation levels)\n";

    return 0;
}
```

3. 编写使用 INT32 累加进行 INT8 矩阵乘法的 C++ 程序，这正是嵌入式机器学习加速器运行的实际计算。
```cpp
// task3_int8_matmul.cpp
// Compile: g++ -O3 -o task3 task3_int8_matmul.cpp

#include <iostream>
#include <chrono>
#include <vector>
#include <cstdint>

// INT8 matmul with INT32 accumulation (what Tensor Cores and MCU accelerators do)
void matmul_int8(const int8_t* A, const int8_t* B, int32_t* C,
                 int M, int N, int K) {
    for (int i = 0; i < M; i++) {
        for (int j = 0; j < N; j++) {
            int32_t sum = 0;
            for (int k = 0; k < K; k++) {
                sum += (int32_t)A[i * K + k] * (int32_t)B[k * N + j];
            }
            C[i * N + j] = sum;
        }
    }
}

// Float32 matmul for comparison
void matmul_f32(const float* A, const float* B, float* C,
                int M, int N, int K) {
    for (int i = 0; i < M; i++) {
        for (int j = 0; j < N; j++) {
            float sum = 0.0f;
            for (int k = 0; k < K; k++) {
                sum += A[i * K + k] * B[k * N + j];
            }
            C[i * N + j] = sum;
        }
    }
}

int main() {
    const int M = 128, N = 128, K = 128;

    std::vector<int8_t> A_i8(M * K, 1), B_i8(K * N, 1);
    std::vector<int32_t> C_i32(M * N);

    std::vector<float> A_f32(M * K, 1.0f), B_f32(K * N, 1.0f);
    std::vector<float> C_f32(M * N);

    // Benchmark INT8
    auto start = std::chrono::high_resolution_clock::now();
    for (int t = 0; t < 100; t++) {
        matmul_int8(A_i8.data(), B_i8.data(), C_i32.data(), M, N, K);
    }
    auto end = std::chrono::high_resolution_clock::now();
    double i8_ms = std::chrono::duration<double, std::milli>(end - start).count() / 100;

    // Benchmark FP32
    start = std::chrono::high_resolution_clock::now();
    for (int t = 0; t < 100; t++) {
        matmul_f32(A_f32.data(), B_f32.data(), C_f32.data(), M, N, K);
    }
    end = std::chrono::high_resolution_clock::now();
    double f32_ms = std::chrono::duration<double, std::milli>(end - start).count() / 100;

    double gflops_i8 = 2.0 * M * N * K / i8_ms / 1e6;
    double gflops_f32 = 2.0 * M * N * K / f32_ms / 1e6;

    std::cout << "INT8 matmul:  " << i8_ms << " ms (" << gflops_i8 << " GOPS)\n";
    std::cout << "FP32 matmul:  " << f32_ms << " ms (" << gflops_f32 << " GFLOPS)\n";
    std::cout << "INT8 speedup: " << f32_ms / i8_ms << "x\n";
    std::cout << "Memory: INT8 = " << M*K + K*N << " bytes vs FP32 = "
              << (M*K + K*N) * 4 << " bytes (4x less)\n";

    return 0;
}
```
