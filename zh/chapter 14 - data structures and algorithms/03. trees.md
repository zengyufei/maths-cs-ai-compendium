# 树

*树是文件系统、数据库、编译器和无数面试题背后的层级数据结构。本节涵盖二叉树、BST、平衡树、Trie、线段树、Fenwick 树和并查集，配合遍历模式、递归思维与难度递进的问题。*

- **树（tree）**是连通无环图，第 13 章。最重要的变体是**二叉树（binary tree）**：每个节点至多有两个子节点，左与右。树无处不在：编译器解析树、浏览器 DOM 树、机器学习决策树和数据库 B 树。

- 树问题的关键洞见是：**大多数树问题用递归解决（most tree problems are solved recursively）**。结构本身就是递归的，一棵树是根与两个子树，因此解法也应如此。掌握“解决左子树、解决右子树、合并”的模式，就能解决大多数树问题。

## 二叉树遍历

- 有四种访问每个节点的标准方式：

    - **中序（Inorder）**，左、根、右：对 BST 按排序顺序访问节点。
    - **前序（Preorder）**，根、左、右：用于序列化和复制树。
    - **后序（Postorder）**，左、右、根：用于删除和计算大小。
    - **层序（Level-order）**，BFS：使用队列逐层访问节点。

```python
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def inorder(root):
    if not root:
        return []
    return inorder(root.left) + [root.val] + inorder(root.right)

def preorder(root):
    if not root:
        return []
    return [root.val] + preorder(root.left) + preorder(root.right)

def postorder(root):
    if not root:
        return []
    return postorder(root.left) + postorder(root.right) + [root.val]

from collections import deque

def level_order(root):
    if not root:
        return []
    result, queue = [], deque([root])
    while queue:
        level = []
        for _ in range(len(queue)):
            node = queue.popleft()
            level.append(node.val)
            if node.left:
                queue.append(node.left)
            if node.right:
                queue.append(node.right)
        result.append(level)
    return result
```

- **陷阱**：上面的递归遍历在每步创建新列表，源于 `+` 拼接，因此为 $O(n^2)$。要提高效率，传入结果列表并原地追加：

```python
def inorder_efficient(root, result=None):
    if result is None:
        result = []
    if root:
        inorder_efficient(root.left, result)
        result.append(root.val)
        inorder_efficient(root.right, result)
    return result
```

### 简单：二叉树最大深度

```python
def max_depth(root):
    if not root:
        return 0
    return 1 + max(max_depth(root.left), max_depth(root.right))
```

- **递归模式**：基例，null → 0；对子节点递归；合并，1 + max。相同模式适用于数十种树问题。

### 简单：翻转二叉树

```python
def invert_tree(root):
    if not root:
        return None
    root.left, root.right = invert_tree(root.right), invert_tree(root.left)
    return root
```

### 中等：最近公共祖先

- **问题**：找出同时为 $p$ 和 $q$ 祖先的最低节点。

- **模式**：若 $p$ 与 $q$ 均在左子树，LCA 位于左子树；均在右子树则位于右子树；若分开，一个左一个右，当前节点就是 LCA。

```python
def lowest_common_ancestor(root, p, q):
    if not root or root == p or root == q:
        return root

    left = lowest_common_ancestor(root.left, p, q)
    right = lowest_common_ancestor(root.right, p, q)

    if left and right:
        return root  # p and q are in different subtrees
    return left if left else right
```

- **陷阱**：该实现假设 $p$ 与 $q$ 均存在于树中。若它们可能不存在，需要额外检查。

### 困难：二叉树最大路径和

- **问题**：找出任意两个节点间路径的最大和，路径不必经过根。

```python
def max_path_sum(root):
    best = [float('-inf')]

    def dfs(node):
        if not node:
            return 0
        left = max(dfs(node.left), 0)   # ignore negative paths
        right = max(dfs(node.right), 0)

        # path through this node (possibly as the "bend")
        best[0] = max(best[0], node.val + left + right)

        # return the max gain this node can contribute to its parent
        return node.val + max(left, right)

    dfs(root)
    return best[0]
```

- **关键洞见**：每个节点有两个问题：(1) *经过（through）*该节点的最佳路径是什么，左 + 节点 + 右？(2) 该节点能向其*父节点（parent）*贡献的最佳路径是什么，节点 + max(左，右)，因为路径不能在两层处分叉。混淆二者是最常见错误。

## 二叉搜索树，BST

- **BST** 满足：对每个节点，左子树全部值均小于它，右子树全部值均大于它。这使搜索、插入和删除均为 $O(\log n)$，在树平衡时。

```python
def search_bst(root, target):
    if not root:
        return None
    if target < root.val:
        return search_bst(root.left, target)
    elif target > root.val:
        return search_bst(root.right, target)
    else:
        return root

def insert_bst(root, val):
    if not root:
        return TreeNode(val)
    if val < root.val:
        root.left = insert_bst(root.left, val)
    else:
        root.right = insert_bst(root.right, val)
    return root
```

- **陷阱**：BST 操作只有树平衡时才是 $O(\log n)$。按排序顺序插入构建的 BST 会退化为链表：每个操作 $O(n)$。这正是 AVL、红黑树等平衡 BST 存在的原因。

### 中等：验证二叉搜索树

```python
def is_valid_bst(root, lo=float('-inf'), hi=float('inf')):
    if not root:
        return True
    if root.val <= lo or root.val >= hi:
        return False
    return (is_valid_bst(root.left, lo, root.val) and
            is_valid_bst(root.right, root.val, hi))
```

- **陷阱**：仅检查 `left.val < root.val < right.val` 是错误的。约束要求左子树*所有*节点都更小，而不只是直接子节点；`lo`/`hi` 边界将该约束向下传递。

### 中等：BST 中第 K 小元素

- **模式**：BST 的中序遍历按排序顺序访问节点，第 $k$ 个访问的节点即答案。

```python
def kth_smallest(root, k):
    count = [0]
    result = [None]

    def inorder(node):
        if not node or result[0] is not None:
            return
        inorder(node.left)
        count[0] += 1
        if count[0] == k:
            result[0] = node.val
            return
        inorder(node.right)

    inorder(root)
    return result[0]
```

## Trie，前缀树

- **Trie** 逐字符在树中保存字符串。每条边代表一个字符，从根到标记节点的路径代表已保存字符串。无论保存多少字符串，Trie 都以 $O(L)$ 查找，$L$ 为字符串长度。

```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.is_end = False

class Trie:
    def __init__(self):
        self.root = TrieNode()

    def insert(self, word):
        node = self.root
        for char in word:
            if char not in node.children:
                node.children[char] = TrieNode()
            node = node.children[char]
        node.is_end = True

    def search(self, word):
        node = self.root
        for char in word:
            if char not in node.children:
                return False
            node = node.children[char]
        return node.is_end

    def starts_with(self, prefix):
        node = self.root
        for char in prefix:
            if char not in node.children:
                return False
            node = node.children[char]
        return True
```

- **何时使用**：自动补全、拼写检查、文字游戏、IP 路由表。凡是需要基于前缀的操作。

### 困难：单词搜索 II

- **问题**：给定字符棋盘和单词列表，找出可通过遍历相邻单元格组成的全部单词。

- **模式**：由单词列表构建 Trie，然后从每个单元格使用 Trie 进行 DFS，提前剪枝，若没有单词以当前前缀开始则停止。

- **陷阱**：没有 Trie 时，每个单词都要单独 DFS：$O(w \cdot m \cdot n \cdot 4^L)$。Trie 在单词间共享前缀计算，显著减少工作量。

## 并查集，不相交集合并

- **并查集（Union-Find）**跟踪不相交集合的集合。两个操作：`find(x)` 返回 $x$ 所在集合的代表元；`union(x, y)` 合并包含 $x$ 与 $y$ 的集合。

```python
class UnionFind:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n
        self.count = n  # number of connected components

    def find(self, x):
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # path compression
        return self.parent[x]

    def union(self, x, y):
        rx, ry = self.find(x), self.find(y)
        if rx == ry:
            return False  # already connected
        # union by rank
        if self.rank[rx] < self.rank[ry]:
            rx, ry = ry, rx
        self.parent[ry] = rx
        if self.rank[rx] == self.rank[ry]:
            self.rank[rx] += 1
        self.count -= 1
        return True
```

- 使用路径压缩和按秩合并后，两种操作的摊销复杂度均为 $O(\alpha(n)) \approx O(1)$，反阿克曼函数，实际近似常数。

- **何时使用**：连通分量、无向图环检测、Kruskal MST、等价项目分组。

### 中等：连通分量数量

```python
def count_components(n, edges):
    uf = UnionFind(n)
    for u, v in edges:
        uf.union(u, v)
    return uf.count
```

### 中等：冗余连接

- **问题**：找出删除后能使图成为树的边，即造成环的边。

- **模式**：逐条处理边。第一个两端点已在同一分量内的边造成环。

```python
def find_redundant(edges):
    uf = UnionFind(len(edges) + 1)
    for u, v in edges:
        if not uf.union(u, v):
            return [u, v]  # already connected → this edge creates a cycle
```

## 线段树与 Fenwick 树

- **线段树（Segment trees）**回答区间查询，子数组上的和、最小值、最大值，并支持点更新，二者均为 $O(\log n)$。

- **Fenwick 树（Fenwick trees）**，二元索引树，是前缀和查询与点更新更简单、更快的替代方案。它使用巧妙位操作：每个位置保存部分和，覆盖范围由最低置位决定。

```python
class FenwickTree:
    def __init__(self, n):
        self.n = n
        self.tree = [0] * (n + 1)

    def update(self, i, delta):
        i += 1  # 1-indexed
        while i <= self.n:
            self.tree[i] += delta
            i += i & (-i)  # add lowest set bit

    def prefix_sum(self, i):
        i += 1
        total = 0
        while i > 0:
            total += self.tree[i]
            i -= i & (-i)  # remove lowest set bit
        return total

    def range_sum(self, l, r):
        return self.prefix_sum(r) - (self.prefix_sum(l - 1) if l > 0 else 0)
```

- **何时使用**：需要带更新的重复区间查询。仅需要前缀和时，优先 Fenwick 树；需要任意区间操作，最小、最大、GCD，时，使用线段树。

---

## 常见陷阱汇总

| 陷阱 | 示例 | 修复 |
|---------|---------|-----|
| BST 只检查直接子节点 | `left.val < root.val` 漏掉深层违规 | 传递 `lo`/`hi` 边界 |
| 递归中的 $O(n^2)$ 列表拼接 | `inorder(left) + [val] + inorder(right)` | 追加到共享列表 |
| 忘记基例 | 空树上的无限递归 | `if not root: return` |
| 混淆经过路径与到父节点路径 | 最大路径和：在两层处分叉 | 向父节点返回单分支，单独跟踪双分支 |
| Fenwick 的 1 索引与 0 索引 | 树数组 off-by-one | 入口处始终 `i += 1` |
| 并查集未用路径压缩 | 最坏情形每次 find 为 $O(n)$ | `self.parent[x] = self.find(self.parent[x])` |

---

## 课后题（NeetCode）

### 二叉树模式
- [Invert Binary Tree](https://neetcode.io/problems/invert-a-binary-tree) — 基础递归
- [Maximum Depth of Binary Tree](https://neetcode.io/problems/depth-of-binary-tree) — 递归深度
- [Same Tree](https://neetcode.io/problems/same-binary-tree) — 同步遍历
- [Subtree of Another Tree](https://neetcode.io/problems/subtree-of-a-binary-tree) — 嵌套递归
- [Binary Tree Level Order Traversal](https://neetcode.io/problems/level-order-traversal-of-binary-tree) — 带层级跟踪的 BFS
- [Binary Tree Maximum Path Sum](https://neetcode.io/problems/binary-tree-maximum-path-sum) — 带全局最优的 DFS
- [Serialize and Deserialize Binary Tree](https://neetcode.io/problems/serialize-and-deserialize-binary-tree) — 前序 + null 标记

### BST 模式
- [Validate Binary Search Tree](https://neetcode.io/problems/valid-binary-search-tree) — 边界传播
- [Kth Smallest Element in a BST](https://neetcode.io/problems/kth-smallest-integer-in-bst) — 中序遍历
- [Lowest Common Ancestor of a BST](https://neetcode.io/problems/lowest-common-ancestor-in-binary-search-tree) — 利用 BST 顺序

### Trie
- [Implement Trie](https://neetcode.io/problems/implement-prefix-tree) — 基本 Trie 操作
- [Design Add and Search Words](https://neetcode.io/problems/design-word-search-data-structure) — Trie + 带通配符的 DFS
- [Word Search II](https://neetcode.io/problems/search-for-word-ii) — Trie 引导的回溯

### 并查集
- [Number of Connected Components](https://neetcode.io/problems/count-connected-components) — 基础并查集
- [Redundant Connection](https://neetcode.io/problems/redundant-connection) — 通过并查集检测环
